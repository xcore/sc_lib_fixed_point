/*
 * Function that computes a sqrt of a 64 bit number stored in two 32 bit numbers.
 * \param h   most significant 32 bits
 * \param l   least significant 32 bits
 * \returns   sqrt
 */
unsigned int sqrtuint2(unsigned int h, unsigned int l);
